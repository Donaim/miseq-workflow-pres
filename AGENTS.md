
# FAQ

---

The main file is `slides.md`.

---

Directory `deps.local` contains repositories should be studied if any context or background is needed.
It includes projects like `MiCall` with its watcher code.

---

Elements such as `<DRAFT>` and `<NOTE>` are used to indicate sections that are still being worked on or contain development notes. In the final version, these should be removed or replaced with finalized content.

---

File `current.md` contains a bunch of useful information for current tasks.

---

